# 공부요정 (study-app) — 프로젝트 컨텍스트

이 파일은 Claude Code가 세션 시작 시 자동으로 읽는 프로젝트 지침입니다.
작업 전에 이 문서를 먼저 확인하세요.

## 1. 프로젝트 개요

초등학교 3~6학년이 **집에서 스스로** 하는 웹 학습 앱. 개발자 ROY가 자녀를 위해 만들며,
다른 초등학생도 쓸 수 있게 공개 배포한다.

- 배포: GitHub Pages — <https://windhak.github.io/study-app/>
- 저장소: `windhak/study-app` (main 브랜치가 곧 배포본)
- **main에 push하면 즉시 배포된다.** 아이가 쓰는 앱이므로 검수 없이 main에 직접 넣지 않는다.

## 2. 기술 원칙 (반드시 지킬 것)

| 항목 | 규칙 | 이유 |
|---|---|---|
| 프레임워크 | 쓰지 않는다. 순수 HTML/CSS/JS | 빌드 없이 파일만 열어도 동작 |
| JS 문법 | **ES5만** (`var`, `function`). 화살표 함수·`let`·`const`·템플릿 리터럴·`Set`/`Map` 금지 | 구형 갤럭시 S6 브라우저 호환 |
| 데이터 저장 | 브라우저 `localStorage`만. 서버·백엔드·외부 API 없음 | 로그인 없이 기기별 저장, 오프라인 동작 |
| 의존성 | 추가하지 않는다 (CDN 포함) | 오프라인·저사양 기기 |

## 3. 파일 구조

```
index.html          화면 11개 (프로필·홈·요정이·끝말잇기 3화면·난이도·설정·퀴즈·결과·기록)
style.css           스타일. CSS 변수로 과목별 색상 관리
data.js             문제은행(국어/영어 낱말, 국어 문장 빈칸·상황, 영어 문장) + 수학/구구단 생성기 + 챗봇 응답
                    (ENGLISH_WORDS 10단계×100, ENGLISH_SENTENCES 10단계×20)
data-wordchain.js   끝말잇기 사전 10,000낱말 (2티어). 별도 파일로 분리 유지
app.js              화면 전환, 프로필/퀴즈/기록/챗봇/끝말잇기 로직
```

## 4. 운영 규칙

- **코드가 바뀌면 `README.md`도 같은 커밋에서 갱신한다.** (예외 없음)
- **브랜치 작업 필수.** `main`/`master`에 직접 커밋·push 금지. 작업은 `<type>/<설명>` (kebab-case,
  type은 `feat`/`fix`/`docs`/`refactor`/`chore`). 병합은 PR로, 사람 검수 후.
- **커밋 메시지**: Conventional Commits (`<type>: <요약>`). 본문 끝에 빈 줄 하나 두고
  `Co-authored-by: Claude <noreply@anthropic.com>` 트레일러를 넣는다.
  git author/committer는 ROY 본인으로 둔다(신원 위조 금지).
- **파괴적 작업 금지**: force push, 브랜치·저장소 삭제는 사람이 한다.
- 문서(PRD·설계문서·결정사항 로그)는 Notion에서 버전별로 관리한다.

## 5. 버전 이력

| 버전 | 내용 |
|---|---|
| v1.0 | MVP — 프로필, 수학/국어 낱말/영어 단어/구구단, 난이도 5단계, 시간·문제수 설정, 기록, 스티커 |
| v1.1 | 프로필 사진 업로드 (canvas 리사이즈 후 base64로 localStorage 저장) |
| v1.2 | 마스코트 챗봇 "요정이" — 규칙 기반 키워드 매칭. 실제 AI 아님, 외부 호출 없음 |
| v1.3 | 영어 단어 발음 듣기 (Web Speech API, 🔊) |
| v1.4 | 국어 문장 연습 — 1~2단계 빈칸 채우기, 3~5단계 생활 상황 판단 객관식 |
| v1.5 | 문제은행 700개 이상으로 확장 |
| v1.6 | 끝말잇기 신설 (사전 1,497낱말) |
| v1.7 | 끝말잇기 확장(10,000낱말) + 자유 입력 허용 + 화면 문구 맞춤법 검수 |
| v1.8 | 영어 단어 1,000낱말·영어 문장 200문항으로 확장 + 영어만 학년제 10단계(초1~고3) + 문제당 1분 제한(카운트다운·초과 시 오답) + 다음 문제 전 정답 3초 표시 |

### 난이도 단계 (v1.8~)

- **영어(단어·문장)만 10단계**로, 학년제다: 1=초1, 2=초3, 3=초4, 4=초6, 5=중1, 6=중2, 7=중3, 8=고1, 9=고2, 10=고3.
  라벨은 `ENGLISH_GRADE_LABELS`(app.js), 과목 판별은 `isEnglishSubject()`, 단계 상한은 `maxLevelFor()`.
- **수학·국어 낱말·국어 문장·구구단은 기존 5단계** 그대로. 난이도 선택기는 `goToDifficulty()`에서 과목별로 분기한다.
- 영어 문장 데이터는 `ENGLISH_SENTENCES`(data.js): 1~5단계 `type:"fill"`(빈칸+영단어 4지선다), 6~10단계 `type:"meaning"`(문장 뜻 4지선다). `choices`를 데이터에 함께 담는다.
- **문제당 제한시간**은 `QUESTION_SECONDS`(=60), 표시는 `#quizQTimer`. 초과 시 `onQuestionTimeout()`이 오답 처리한다. 세션 시간/문제수 모드는 그대로 두고 문제당 타이머를 추가로 얹은 구조다. 정답 표시 시간은 `REVEAL_MS`(=3000). 끝말잇기는 자체 60초 타이머라 제외.

## 6. 끝말잇기 핵심 규칙 (건드릴 때 주의)

- **주제 12개**. 주제를 골라도 아무 낱말이나 낼 수 있고, 주제가 맞으면 보너스 +5점.
- **아이의 낱말은 사전에 없어도 인정한다.** 거부는 세 가지뿐 — 끝말 불일치 / 한글 2글자 미만 /
  이미 나온 낱말. 어느 경우도 감점·패배가 아니고 타이머를 유지한 채 재입력받는다.
  사전에 없으면 요정이가 "뜻은 모르겠지만 이어갈게"라고 답한 뒤 다음 낱말을 낸다.
- **두음법칙 양방향 허용** (`달력 → 역사`, `정리 → 이슬`). 초성·중성으로 판정.
- **중복 금지**는 한 게임(라운드 누적 포함) 단위.
- **요정이 낱말 선택 순서**: ① 한방단어 제외 → ② 80% 잇기 쉬운 낱말 / 20% 어려운 낱말
  → ③ 초등 어휘(`WORDCHAIN_EASY`) 우선 → ④ 2~3글자 우선. 결과적으로 86%가 초등 어휘.
- **요정이가 막히면 라운드 승리** +30점을 주고 새 낱말로 계속. 게임은 60초 초과 또는
  "모르겠어요" 버튼으로만 끝난다.
- 점수 = `이어간 낱말 × 10 + 주제 보너스 × 5 + 라운드 승리 × 30`. 스티커 100/200/350점.
- 기록은 `subject: "wordchain"`, 주제명을 `level` 자리에 저장. **평균 정답률 통계에서 제외**한다
  (정답률 개념이 없어 다른 과목 통계를 왜곡함).

## 7. 사전 (`data-wordchain.js`)

- **출처**: 국립국어원 「한국어기초사전」 <https://krdict.korean.go.kr>
  (`spellcheck-ko/korean-dict-nikl` 배포본을 가공)
- **라이선스**: 낱말 데이터는 **CC BY-SA 4.0** (원자료 CC BY-SA 2.0 KR). 출처 표기를 파일 헤더와
  README에 유지할 것. 낱말 데이터는 이 파일에만 격리한다.
- **구조**: Tier 1 = 6,921낱말(초등~중급, 주제 태그 있음) / Tier 2 = 3,079낱말(확장, 태그 없음).
  `*` 접두사는 요정이가 우선 쓰는 초등 어휘 표시. 인덱스는 `wcEnsureIndex()`로 끝말잇기 첫 진입 시 1회 생성
  (앱 시작 비용을 27ms → 2ms로 줄이기 위함).
- **아동 안전 필터**: 의미범주 `정치와 행정 > 사법 및 치안 행위`, `인간 > 생리 현상`,
  `삶 > 병과 증상`, `종교`를 통째로 배제 + 차단 낱말 약 200개.
  **낱말을 추가할 때 이 필터를 반드시 통과시킬 것.** 일반 국어사전을 그대로 쓰면
  비하어·범죄어·실존 인물명이 섞여 들어온다(실제로 `꼽추`, `김정은`이 검출된 전례 있음).
- ⚠️ **이 파일은 로컬에서 재생성할 수 없다.** 원본 사전 XML 369MB를 내려받아 가공한 결과물이다.
  수정이 필요하면 파일을 직접 편집하거나 웹 채팅 쪽 Claude에게 재생성을 요청한다.

## 8. 화면 문구 규칙

- 문구는 국립국어원 기반 hunspell 한국어 사전으로 검수했다. 새 문구를 넣을 때도 맞춤법을 확인한다.
- **조사는 함수로 고른다.** 직접 `"은"`, `"이"`, `"(으)로"`를 하드코딩하지 말 것.
  - `josa(값, "은", "는")` — 받침 유무로 선택. 숫자도 한글 독음 기준으로 판정(3→삼→"3이에요")
  - `josaRo(값)` — `로`/`으로` (ㄹ 받침 뒤는 `로`)
  - 끝말잇기 문구는 `{W}`, `{은는}`, `{이가}` 자리표시자를 쓰고 `wcFillWord()`로 채운다
- **보조 용언은 띄어 쓴다** (걸어 보세요, 풀어 봐요, 골라 보세요).
- `sameAnswer(a, b)`로 정답을 비교한다. 띄어쓰기 차이는 정답으로 인정한다
  (정답이 `화석연료`여도 아이가 `화석 연료`라고 쓰면 맞음).
- ⚠️ **`data.js`의 `keywords` 배열은 맞춤법을 고치지 말 것.** `잘가`, `몇개`, `로블럭스`처럼
  아이가 틀리게 적어도 챗봇이 알아듣게 하려고 일부러 넣은 매칭용 표기다.
  같은 낱말이 화면에 표시되는 쪽(`replies`)만 바른 표기를 쓴다.

## 9. 알려진 함정

- **GitHub MCP 커넥터 쓰기는 실패한다** — `create_or_update_file`/`push_files`가
  "No approval received"로 거부됨(collaborator 권한이 있어도). 커밋은 로컬 `git`/`gh`로 한다.
- claude-in-chrome 브라우저 자동화는 동작하지만 불안정하다(MCP 서버 무응답 전례). 최후 수단.
- 채점은 문자열 완전 일치 기반이므로 정답 표기를 바꿀 때 기존 기록과의 호환을 확인한다.

## 10. 미결 사항

- **일별 사용시간 통계**: 구현 가능성 검토 완료(localStorage에 데이터 이미 있음). 미착수.
- **사용시간 초과 시 이메일 알림**: EmailJS 자동 발송 방식으로 결정. ROY가 Public Key /
  Service ID / Template ID를 제공하면 착수. **그전에는 코딩하지 않는다.**
- **끝말잇기 점수 부풀리기**: 사전 검사를 뺐으므로 뜻 없는 한글 조합으로도 10점을 받을 수 있다.
  필요하면 사전에 없는 낱말은 5점으로 차등을 두는 방안이 있다(미적용).
- **낱말 뜻 보기**: 사전에 뜻 필드가 없어 미구현.

## 11. 작업 방식

- ROY는 **코딩 전에 구현 가능성·제약·결정 사항을 단계적으로 확인받는 것을 선호한다.**
  모호하면 임의로 정하지 말고 1~3개 질문으로 먼저 확정한다.
- 국어 문장 연습의 상황 판단 문제는 문법이 아니라 **아이가 실제로 겪는 생활 상황**
  (또래 갈등, 온라인 안전, 분실물, 모둠 활동)으로 만든다. 정답은 신뢰할 수 있는 어른에게
  알리기·정직·건설적 해결 방향으로 둔다.
- 변경 후에는 브라우저에서 실제로 동작을 확인한다. 로직 변경 시 간단한 검증 스크립트를 돌린다.
